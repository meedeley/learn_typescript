"use strict";
describe("Data Type", () => {
    it("should must declare", () => {
        let name = "Nichola Saputra";
        expect(name).toBe("Nichola Saputra");
        let balance = 1000000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
